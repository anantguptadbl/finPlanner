extends ../_bootstrap
append styles
	// Custom styles for this template
	link(href='/stylesheets/dashboard.css', rel='stylesheet')
	script(src='https://d3js.org/d3.v4.js')
block body  
	nav.navbar.navbar-inverse.navbar-fixed-top
		.container-fluid
			.navbar-header
				button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
					span.sr-only Toggle navigation
					span.icon-bar
					span.icon-bar
					span.icon-bar
				a.navbar-brand(href='/dashboard') Project name
			#navbar.navbar-collapse.collapse
				ul.nav.navbar-nav.navbar-right
					li.active
						a(href='/dashboard') Dashboard
					li
						a(href='/profile') Profile
					li
						a(href='/contact') Contact
				form.navbar-form.navbar-right
					input.form-control(type='text', placeholder='Search...')
	.container-fluid
		.row
			.col-sm-3.col-md-2.sidebar
				ul.nav.nav-sidebar
					li.headingTag
						p Calculators
					li.active
						a(href='#pfcalculator') PF Calculator
					li.active
						a(href='#loancalculator') Loan Calculator

				ul.nav.nav-sidebar
					li
						a(href='') Nav item
					li
						a(href='') Nav item again
					li
						a(href='') One more nav
					li
						a(href='') Another nav item
					li
						a(href='') More navigation
				ul.nav.nav-sidebar
					li
						a(href='') Nav item again
					li
						a(href='') One more nav
					li
						a(href='') Another nav item
			.col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
				//h1.page-header Dashboard
				//.row.placeholders
				//	.col-xs-6.col-sm-3.placeholder
				//		img.img-responsive(src='../images/dashboard_blue.jpg', alt='Generic placeholder thumbnail')
				//		h4 Label
				//		span.text-muted Something else
				//	.col-xs-6.col-sm-3.placeholder
				//		img.img-responsive(src='../images/dashboard_green.jpg', alt='Generic placeholder thumbnail')
				//		h4 Label
				//		span.text-muted Something else
				//	.col-xs-6.col-sm-3.placeholder
				//		img.img-responsive(src='../images/dashboard_blue.jpg', alt='Generic placeholder thumbnail')
				//		h4 Label
				//		span.text-muted Something else
				//	.col-xs-6.col-sm-3.placeholder
				//		img.img-responsive(src='../images/dashboard_green.jpg', alt='Generic placeholder thumbnail')
				//		h4 Label
				//		span.text-muted Something else
				//
				div.target 
					div#pfcalculator
						h2.sub-header PF Calculator
						p We will be calculating the PF amount

						p CurrentDate
						input(type="text" name="date" placeholder="")
						p Current PF Balance
						input(type="text" name="currentBalance" placeholder="")
						p Monthly PF Deposits
						input(type="text" name="currentBalance" placeholder="")

						div#chart1
							script(type="text/javascript").
								var margin = {top: 10, right: 30, bottom: 30, left: 60},
									width = 460 - margin.left - margin.right,
									height = 400 - margin.top - margin.bottom;

								var svg = d3.select("#chart1")
								.append("svg")
									.attr("width", width + margin.left + margin.right)
									.attr("height", height + margin.top + margin.bottom)
								.append("g")
									.attr("transform",
										"translate(" + margin.left + "," + margin.top + ")");

								d3.csv("https://raw.githubusercontent.com/holtzy/data_to_viz/master/Example_dataset/3_TwoNumOrdered_comma.csv",

								function(d){
									return { date : d3.timeParse("%Y-%m-%d")(d.date), value : d.value }
								},

								function(data) {

									var x = d3.scaleTime()
									.domain(d3.extent(data, function(d) { return d.date; }))
									.range([ 0, width ]);
									svg.append("g")
									.attr("transform", "translate(0," + height + ")")
									.call(d3.axisBottom(x));

									var y = d3.scaleLinear()
									.domain([0, d3.max(data, function(d) { return +d.value; })])
									.range([ height, 0 ]);
									svg.append("g")
									.call(d3.axisLeft(y));

									svg.append("path")
									.datum(data)
									.attr("fill", "none")
									.attr("stroke", "steelblue")
									.attr("stroke-width", 1.5)
									.attr("d", d3.line()
										.x(function(d) { return x(d.date) })
										.y(function(d) { return y(d.value) })
										)
								});

					div#loancalculator
						h2.sub-header Loan Calculator
						p We will be calculating the Loan Payouts, Pending, Interest
				
							